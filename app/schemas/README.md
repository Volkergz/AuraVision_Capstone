# app/schemas

Define los esquemas de entrada/salida usados por la API (p. ej. Pydantic).

Propósito:
- Validar datos entrantes.
- Serializar/deserializar objetos para la API.
- Mantener contratos estables entre cliente y servidor.

Ejemplos de uso: `user`, `auth`, `device`, `event`, `alert` schemas.