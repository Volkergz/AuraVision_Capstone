# app/models

Contiene los modelos de dominio persistentes (ORM).

Ejemplos:
- `user.py`, `device.py`, `event.py`, `alert.py`.

Responsabilidad: definir la estructura de las tablas y relaciones, y proporcionar métodos de conveniencia relacionados con la persistencia si es necesario.